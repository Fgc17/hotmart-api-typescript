{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["CLIENT_ID", "CLIENT_SECRET", "BASIC"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
