export interface Shopper {
  /**Buyer's email address. */
  email: string;

  /**Buyer's phone number. */
  phone: string;
}

export interface Subscriber {
  name: string;
  email: string;
  ucode: string;
}
